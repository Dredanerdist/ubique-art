<%= simple_form_for(@painting_at_spot) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :from_date%>
    <%= f.input :to_date %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Eintragen"%>
  </div>
<% end %>
