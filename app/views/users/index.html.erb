#encoding: utf-8
<h3>Mitglieder</h3>
<div class="span8">
<table class="table table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Dabei seit</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user %></td>
      <td><%= user.email %></td>
      <td><%= user.created_at.to_date %></td>
			<% if can? :manage, user %>
      <td>
        <a data-toggle="modal" href="#role-options-<%= user.id %>" class="btn btn-mini" type="button">Adminstatus verwalten</a>
        <%= render user %>
      </td> 
			<% end %>
			<% if can? :manage, user then%>
      	<td><%= link_to("Nutzer löschen", user_path(user), :data => { :confirm => "Sicher?" }, :method => :delete, :class => 'btn btn-mini') unless user == current_user %></td>
			<% end %>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
